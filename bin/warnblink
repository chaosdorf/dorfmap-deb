#!/bin/sh

GPIO=3

if [ "$1" = yellow ]; then
	GPIO=22
fi

for i in $(seq 1 57); do
	echo 1 > /sys/class/gpio/gpio"${GPIO}"/value
	sleep 0.5
	echo 0 > /sys/class/gpio/gpio"${GPIO}"/value
	sleep 0.5
done
